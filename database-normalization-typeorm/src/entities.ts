import { BaseDataSourceOptions } from 'typeorm/data-source/BaseDataSourceOptions';

/**
 * TODO add entities to this list after defining them in /entity
 *
 * TODO the complete database schema should at least conform to 3rd Normal Form
 */
export const entities: BaseDataSourceOptions['entities'] = [];
