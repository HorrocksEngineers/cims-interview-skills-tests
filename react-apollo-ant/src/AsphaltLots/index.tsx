import { Typography } from 'antd';

interface AsphaltLotsProps {}

/**
 * TODO organize the results of `useProjectAsphaltLots` with Ant Design
 *      you do not need to _every_ field, but should use as much as you reasonably can
 * TODO communicate the loading state of the query
 *
 * TODO feel free to create additional components as needed in order to better organize
 */
const AsphaltLots: React.FC<AsphaltLotsProps> = () => {
  return (
    <>
      <Typography.Title>Asphalt Lots</Typography.Title>
    </>
  );
};

export default AsphaltLots;
